query ProductPaths($after: String, $channel: String!) {
  products(first: 5, channel: $channel, after: $after) {
    pageInfo {
      hasNextPage
    }
    edges {
      cursor
      node {
        id
        slug
      }
    }
  }
}
